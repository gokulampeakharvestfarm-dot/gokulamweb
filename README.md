# Gokulam Goshala Website

A responsive custom HTML/CSS/JS starter website.

## Quick setup
1. Put your photos in `assets/` as `hero.jpg` and `about.jpg`.
2. Open `index.html` in a browser.
3. Edit the sample donation/bank/contact details in `index.html` before publishing.
4. Upload the folder to any normal web host.
